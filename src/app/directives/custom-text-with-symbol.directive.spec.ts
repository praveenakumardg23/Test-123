import { CustomTextWithSymbolDirective } from './custom-text-with-symbol.directive';

describe('CustomTextWithSymbolDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTextWithSymbolDirective();
    expect(directive).toBeTruthy();
  });
});
